#include <stdio.h>

int main() {
    int n;
    int reverse = 0;
    int remainder;
    
    int test = (5+3)/2;

    printf("Enter an integer: ");
    scanf("%d", n);

    if (n > 0) {
    	n = 1;
    } else if (n < 0) {
    	n = 2;
    } else if (n == 0) {
    	n = 3;
    } else {
    	n = 3;
    }

    return 0;
}